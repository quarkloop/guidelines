"""
quarkloop guidelines tooling package.

Modules:
  models      — data classes for check results and validation reports
  archetypes  — repository archetype definitions
  generator   — file generation from Jinja2 templates
  checks      — individual validation check functions
  validator   — orchestrates checks and produces reports
  cli         — argument parsing and subcommand dispatch
"""
